# The opt-in live integration tier

Tutti's required CI gate is hermetic: no `claude`, no `gh`, no network. Everything
process- or network-adjacent is covered instead by pure unit tests over its parsing
and prompt-building seams (stream-json parsing, gh-json parsing, artifact reading,
prompt construction, PID locking).

The one place that cannot be fully covered by a pure test is whether `claude -p`
actually behaves the way `ClaudeBackend` expects when invoked against a real,
authenticated `claude` binary. That is covered by a separate, opt-in "live" tier.

## What it is

`crates/tutti-backend-claude/tests/live.rs` is gated behind the `live` cargo feature
and marked `#[ignore]`. It does not compile at all unless `--features live` is passed,
so it can never leak into the required gate. When run, it spawns a real `claude`
process against a throwaway `AgentTask` and asserts the run never panics: it must
land on either `AgentStatus::ReadyToShip` or `AgentStatus::Blocked`.

## Running it

Prerequisites:
- An authenticated `claude` CLI on `PATH` (run `claude --help` to confirm it is
  usable and check current flags before trusting the backend's invocation).
- For the forge-side live tests envisioned by this tier in a future slice, a
  throwaway GitHub repo configured with the `status:*` labels the engine expects.

```bash
cargo test -p tutti-backend-claude --features live -- --ignored
```

## Confirmed runtime facts (captured live 2026-07-11)

These were verified against a real `claude` and a real `gh`, and the backend was fixed
to match. Fixture: `crates/tutti-backend-claude/tests/fixtures/real-stream.jsonl`.

- **`claude -p --output-format stream-json` REQUIRES `--verbose`.** Without it the CLI
  refuses to launch ("requires --verbose"). It is now in `ClaudeBackend`'s default args
  (guarded by the `verbose_is_in_default_args` test).
- **Assistant text/tool_use live in `message.content` as an ARRAY of typed blocks**
  (`[{"type":"text","text":...}]`, `[{"type":"tool_use","name":...}]`), not a top-level
  string. The parser handles the array shape.
- **The `result` event is the authoritative outcome**:
  `{"type":"result","subtype":"success","is_error":false,"api_error_status":null,`
  `"result":"...","usage":{"input_tokens":N,"output_tokens":M}}`. Outcome/error detection
  keys off `is_error` / `api_error_status`, and real token usage is read from `usage`.
- **Rate limits are a structured `rate_limit_event`** with `rate_limit_info.status`
  ("allowed" means NOT limited), not a text substring. Limit detection keys off this.
- **`gh pr checks <n> --json name,state`** returns `[{"name":"...","state":"SUCCESS"}]`
  and exits 0 on all-success (exit 8 pending / 1 failed by design, JSON still on stdout).
  `ci_status` reads stdout regardless of exit via `run_capture`, and `overall_ci_state`
  maps the `state` values. Confirmed against real PR #2.

## Still assumed (confirm on the next live end-to-end run)

- `gh pr checks` exit codes and JSON for the PENDING and FAILURE states (only SUCCESS
  was observed live).
- `gh pr create` stdout shape (the PR URL) under real conditions.
- The full `tutti run` drive against a sandbox repo: claim -> worktree -> implement ->
  review -> PR -> CI -> merge -> record, and `recover_stale`.

## Rules

- Never add this tier, or the `live` feature, to any required CI workflow. It costs
  real tokens, needs a real credentialed `claude`, and is not deterministic enough
  to gate merges on.
- If a change to `ClaudeBackend` needs verifying against real `claude` behavior
  (invocation flags, output shape), run this tier locally before merging, but the
  required gate result is what actually blocks the PR.
